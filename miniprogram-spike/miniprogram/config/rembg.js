const rembgConfig = {
  // Development endpoint. Production mini programs must use an HTTPS domain
  // added to the WeChat uploadFile legal domain list.
  endpoint: "https://api.mixmade.xyz/remove-bg",
  fileFieldName: "file",
  formData: {},
  headers: {}
};

module.exports = rembgConfig;
