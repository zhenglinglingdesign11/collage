# This file is included from nginx.conf's http block via conf.d/*.conf.
# It limits each source IP to 10 requests per minute on the rembg endpoint.
limit_req_zone $binary_remote_addr zone=rembg_ip_rate:10m rate=10r/m;
limit_conn_zone $binary_remote_addr zone=rembg_ip_conn:10m;
